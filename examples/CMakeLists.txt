# CLI11-based demo binaries (asio_example.cpp was removed as dead code)
set(XSL_EXAMPLES tcp_echo udp_echo udp_client http_client http_server)
foreach(_example IN LISTS XSL_EXAMPLES)
  add_executable(${_example} ${_example}.cpp)
  target_link_libraries(${_example} PRIVATE clilib xsl_asio)
endforeach()
